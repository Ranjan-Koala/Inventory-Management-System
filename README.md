# Inventory-Management-System
A Java-based Inventory Management System built using JDBC, Hibernate ORM, Maven, and MySQL. The system manages Products, Categories, Suppliers, and Stock In/Out operations. Hibernate handles advanced queries and entity mapping, while JDBC performs real-time CRUD and database transactions. Maven simplifies build and dependency management.
